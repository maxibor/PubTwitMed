language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python pubmed_twitter_bot.py -doi doi_db.txt -artmax 2 -topic micriobiome -email "random_mail@gmail.com"
